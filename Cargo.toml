[workspace]
resolver = "2"
members = [
    "contracts/token",
    "contracts/escrow",
]

[workspace.dependencies]
soroban-sdk = "21.0.0"

[profile.release]
opt-level = "z"
overflow-checks = true
lto = true
codegen-units = 1
strip = true
debug = false
panic = "abort"
